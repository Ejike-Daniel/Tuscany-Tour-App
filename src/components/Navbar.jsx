import logo from "../assets/logo.png";

const Navbar = () => {
  return (
    <nav>
      <div>
        <img src={logo} alt={logo} />
      </div>
      <div>
        <ul></ul>
      </div>
      <div></div>
    </nav>
  );
};

export default Navbar;
